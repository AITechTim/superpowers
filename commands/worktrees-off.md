---
description: "Disable automatic worktree creation for the rest of this session"
---

For the rest of this session, do NOT auto-create git worktrees when starting feature work or executing plans. Continue working in the current branch.

This overrides the default `using-superpowers` behavior. The user can still explicitly invoke `/worktree` to create one manually. Re-enable auto-creation with `/worktrees-on`.

Acknowledge the toggle and remember it for the rest of the conversation.
